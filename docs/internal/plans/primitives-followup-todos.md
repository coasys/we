# Primitives: Follow-up TODOs

Small items remaining after the tiered architecture refactor (P1–P8).

## A11y

- [ ] **`we-modal` focus trap** — Trap Tab/Shift+Tab within the modal while open, preventing focus from escaping to background content.
- [ ] **`we-input` `aria-labelledby`** — Associate the `<we-text part="label">` element with the `<input>` via `aria-labelledby` so screen readers announce the label.
