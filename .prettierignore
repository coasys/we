**/node_modules
**/dist
**/build
**/target
**/dist-electron
**/.tauri
*.asar
.git
pnpm-lock.yaml

# Machine-generated sources (e.g. packages/models/src/generated/coreManifest.ts)
**/src/generated

# Tauri writes its capability schemas here on every build (`src-tauri/.gitignore` ignores them for the
# same reason). Four of them fail a format check on any machine that has built the Tauri app, which made
# `format:check` fail for a reason nobody could act on.
**/src-tauri/gen

# Auto-generated AI context files (packages/ai-context)
.github/copilot-instructions.md
CLAUDE.md
AGENTS.md
.cursor/rules/we-schema.mdc
packages/ai-context/src/schemaContext.ts
