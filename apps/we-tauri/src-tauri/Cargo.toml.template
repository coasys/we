[package]
name = "we-ad4m-launcher"
version = "0.1.0"
description = "WE AD4M Launcher"
authors = ["James Weir"]
edition = "2021"

[lib]
name = "app_lib"
crate-type = ["staticlib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["devtools"] }
tauri-plugin-opener = "2"
# The file dialog behind export/import. Used from Rust rather than the JS bridge: the path is for
# the executor, which runs in this process, so nothing about it belongs in the renderer.
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# AD4M paths auto-generated from we-seed.json
# To update: modify we-seed.json ad4m.repoPath, then run: node scripts/generate-seed-config.cjs
ad4m-client = { path = "{{AD4M_CLIENT_PATH}}" }
tokio = { version = "1", features = ["full"] }
warp = "0.3"
ad4m-executor = { path = "{{AD4M_EXECUTOR_PATH}}" }
uuid = { version = "0.8", features = ["v4"] }
dirs = "5"

# `enable_media` in src/lib.rs reaches into the GTK webview to grant camera/microphone access,
# which needs webkit2gtk directly — it is not reachable through tauri's re-exports. Linux only,
# matching the `#[cfg(target_os = "linux")]` on that function.
#
# The version is pinned to exactly what wry declares (`=2.0.2`, see wry's own Cargo.toml) so this
# resolves to the same copy tauri's webview already uses. A second webkit2gtk would compile and
# then hand back `WebView` values the running webview does not recognise as its own.
#
# This lived only in a locally-generated Cargo.toml until now: Cargo.toml is gitignored and built
# from this template, so the dependency vanished the next time anyone regenerated it, and
# `use webkit2gtk::…` stopped resolving. The template is the tracked source of truth — a Rust
# dependency that is not in here does not exist.
[target.'cfg(target_os = "linux")'.dependencies]
webkit2gtk = { version = "=2.0.2", features = ["v2_40"] }

# Must mirror ad4m's workspace-root `[patch.crates-io]`.
#
# Holochain 0.7.0-dev.16 needs kitsune2 code that was never published, so ad4m redirects the whole
# family to a git rev. `[patch]` only applies from the workspace root being built, and this crate is
# its own workspace — so without these lines cargo silently resolved the crates.io releases instead.
# They carry the same version strings but different content, which is why the failure surfaced as a
# missing struct field (`CoreBootstrapConfig::auth_material_base64`) rather than a version conflict.
# Keep the rev in step with ad4m's.
[patch.crates-io]
kitsune2 = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_api = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_bootstrap_client = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_bootstrap_srv = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_core = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_dht = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_gossip = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_transport_iroh = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
kitsune2_transport_tx5 = { git = "https://github.com/holochain/kitsune2.git", rev = "320a4d9e622c5f1a6f22d29c8beeb0cb5c333cc3" }
