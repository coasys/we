{
  "name": "@we/cli",
  "version": "0.1.0",
  "description": "CLI tools for WE",
  "author": "James Weir",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "bootstrap": "tsup",
    "build:steps": "tsup",
    "build": "if [ -f ./dist/we-build.js ]; then node ./dist/we-build.js; else pnpm run build:steps; fi",
    "typecheck": "tsc --noEmit"
  },
  "bin": {
    "we-build": "./dist/we-build.js",
    "we-banner": "./dist/we-banner.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsup": "^8.5.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false
}
