{
  "private": true,
  "name": "@we/schema-bench",
  "version": "0.1.0",
  "description": "Headless render benchmarks for the schema system, measured through both a stub and the real design-system registry",
  "type": "module",
  "scripts": {
    "bench": "vitest run",
    "probe": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "@we/components": "workspace:*",
    "@we/primitives": "workspace:*",
    "@we/schema-shared": "workspace:*",
    "@we/schema-solid": "workspace:*",
    "happy-dom": "^20.8.4",
    "solid-js": "^1.9.5",
    "vite-plugin-solid": "^2.11.11",
    "vitest": "^4.1.0"
  }
}
