{
  "name": "@we/template-default",
  "version": "0.1.0",
  "description": "WE's built-in space templates — shipped as data, versioned independently of the framework that renders them",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "dependencies": {
    "@we/schema-shared": "workspace:*",
    "@we/template-kit": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.3.0"
  }
}
