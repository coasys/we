{
  "name": "@we/template-kit",
  "version": "0.1.0",
  "description": "Reusable template fragments — the shapes WE's own templates are built from, as data",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "dependencies": {
    "@we/schema-kit": "workspace:*",
    "@we/schema-shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "vitest": "^4.0.15"
  }
}
