// Typed `we-*` JSX intrinsics, generated from the Custom Elements Manifest. A DX layer only — the
// primitives are Lit custom elements and render as plain tags without it.
import '@we/primitives/solid/types';
