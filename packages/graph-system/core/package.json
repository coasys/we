{
  "name": "@we/graph-core",
  "version": "0.1.0",
  "description": "The graph engine — store, expansion state, viewport, hit-testing and styling, with no framework or backend",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:steps": "tsup",
    "build": "we-build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "@we/graph-protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "@we/cli": "workspace:*",
    "@we/graph-protocol": "workspace:*",
    "tsup": "^8.5.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  },
  "sideEffects": false
}
